name: Release

on:
  push:
    branches:
      - main
      - main-sbx
      - main-dev2
      - main-prd

env:
  REGISTRY: ghcr.io
  REPOSITORY_OWNER: ${{ github.repository_owner }}

permissions:
  contents: write
  packages: write
  attestations: write
  id-token: write

jobs:
  generate-version:
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.out.outputs.version }}

    steps:
      - uses: actions/checkout@v2

      - name: Determine branch prefix
        id: branch
        run: |
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          case "$BRANCH_NAME" in
            main-sbx)
              echo "::set-output name=prefix::sbx_"
              ;;
            main-dev2)
              echo "::set-output name=prefix::dev2_"
              ;;
            main-prd)
              echo "::set-output name=prefix::prd_"
              ;;
            main)
              echo "::set-output name=prefix::"
              ;;
            *)
              echo "::set-output name=prefix::${BRANCH_NAME}_"
              ;;
          esac

      - id: pr
        uses: actions-ecosystem/action-get-merged-pull-request@v1.0.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - uses: zwaldowski/semver-release-action@v2
        with:
          dry_run: true
          bump: ${{ steps.pr.outputs.labels }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prefix: ${{ steps.branch.outputs.prefix }}

      - name: Set version output
        id: out
        run: echo "::set-output name=version::$(echo ${{ steps.branch.outputs.prefix }}${VERSION})"

  # image build&push
  tmforum-apis:
    needs: [ "generate-version" ]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '17'
          java-package: jdk

      - name: Log into GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build&Push image
        run: |
          REPO_LOWER=$(echo "${{ env.REPOSITORY_OWNER }}" | tr '[:upper:]' '[:lower:]')
          mvn versions:set -DnewVersion=${{ needs.generate-version.outputs.version }}
          mvn clean install deploy -Poci -DskipTests -DskipITs -Dimage.tag=${{ needs.generate-version.outputs.version }} -Dimage.registry="${{ env.REGISTRY }}" -Dimage.repository="${REPO_LOWER}"

  git-release:
    needs: ["generate-version", "tmforum-apis"]

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v2

      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: ${{ needs.generate-version.outputs.version }}
          prerelease: false
          title: ${{ needs.generate-version.outputs.version }}
          files: |
            LICENSE
