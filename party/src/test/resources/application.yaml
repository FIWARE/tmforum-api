micronaut:
  caches:
    entities:
      maximumSize: 1000

  metrics:
    enabled: false
    export:
      prometheus:
        step: PT2s
        descriptions: false

  http:
    services:
      read-timeout: 30s
      ngsi:
        path: ngsi-ld/v1
        url: http://localhost:1026
        read-timeout: 30

---
endpoints:
  metrics:
    enabled: false
  health:
    enabled: false

---
loggers:
  levels:
    ROOT: TRACE

---
general:
  contextUrl: https://smartdatamodels.org/context.jsonld